{
  "name": "3dlabel",
  "private": true,
  "type": "module",
  "description": "Config only — marks .js files as ES modules so Node's built-in test runner can import layout.js. No dependencies; the app itself has no build step.",
  "scripts": {
    "test": "node --test"
  }
}
